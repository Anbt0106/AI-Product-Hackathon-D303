# Sao chép file này thành codebase/.env rồi điền key của bạn.
# Không commit file .env.
AI_PROVIDER=openai
OPENAI_API_KEY=

# Mặc định cân bằng chất lượng/chi phí cho demo; có thể đổi nếu tài khoản hỗ trợ.
OPENAI_MODEL=gpt-5.6-terra
PORT=5173
